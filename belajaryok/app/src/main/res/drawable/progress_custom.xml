<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/progress_custom.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background (track) -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />
            <solid android:color="#D3D3D3"/> <!-- Grey track -->
        </shape>
    </item>

    <!-- Secondary progress (optional, bisa diabaikan jika tidak digunakan) -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="10dp" />
                <solid android:color="#A5D6A7"/> <!-- Light green -->
            </shape>
        </clip>
    </item>

    <!-- Progress bar -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dp" />
                <solid android:color="#4CAF50"/> <!-- Green progress -->
            </shape>
        </clip>
    </item>
</layer-list>
